spring:
  datasource:
    url: jdbc:h2:file:./backend/data/springcrudDB
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-console


# http://localhost:8080/h2-console

app:
  security:
    jwt:
      # Base64-encoded secret — min 64 bytes for HS512. Change in production!
      secret: aW52b2ljZS1odWItand0LXNlY3JldC1rZXktZm9yLWRldmVsb3BtZW50LW9ubHktY2hhbmdlLWluLXByb2R1Y3Rpb24h
      expiration-seconds: 86400  # 24 hours